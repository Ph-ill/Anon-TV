<resources>
    <!-- Dark theme that inherits from base theme -->
    <style name="Theme.AnonTv" parent="Theme.Leanback">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        
        <!-- Secondary colors -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        
        <!-- Surface colors -->
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        
        <!-- Background colors -->
        <item name="colorBackground">@color/colorBackground</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>
        
        <!-- Accent and utility colors -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorError">@color/colorError</item>
        
        <!-- Text colors -->
        <item name="colorTextPrimary">@color/colorTextPrimary</item>
        <item name="colorTextSecondary">@color/colorTextSecondary</item>
        <item name="colorTextHint">@color/colorTextHint</item>
        
        <!-- Divider color -->
        <item name="colorDivider">@color/colorDivider</item>
        
        <!-- Gradient references -->
        <item name="backgroundGradient">@drawable/bg_gradient_dark</item>
        <item name="accentGradient">@drawable/accent_gradient_dark</item>
        <item name="cardGradient">@drawable/card_gradient_dark</item>
        
        <!-- Background drawables -->
        <item name="backgroundDrawable">@drawable/bg_gradient_dark</item>
        <item name="cardBackgroundDrawable">@drawable/card_gradient_dark</item>
        
        <!-- Window background -->
        <item name="android:windowBackground">@color/colorBackground</item>
        
        <!-- Status bar and navigation bar -->
        <item name="android:statusBarColor">@color/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/colorPrimaryVariant</item>
    </style>
    
    <!-- Theme for cards and list items -->
    <style name="Theme.AnonTv.Card" parent="Theme.AnonTv">
        <item name="android:background">@drawable/card_gradient_dark</item>
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>
    
    <!-- Theme for focused items -->
    <style name="Theme.AnonTv.Focused" parent="Theme.AnonTv">
        <item name="android:background">@drawable/accent_gradient_dark</item>
        <item name="android:textColor">@color/colorOnSecondary</item>
    </style>
</resources> 